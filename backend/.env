NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/bizone
JWT_SECRET=JWT_SECRET=JKFLs4Mvzm80iABNhTX2XSp36vFrYM3mlOCCDzz/sso=
JWT_EXPIRES_IN=7d

# Payment Gateway (Flutterwave)
FLW_PUBLIC_KEY=your_flutterwave_public_key
FLW_SECRET_KEY=your_flutterwave_secret_key

# Cloudinary for image uploads
CLOUDINARY_CLOUD_NAME=dcojm0hqf
CLOUDINARY_API_KEY=174564861314216
CLOUDINARY_API_SECRET=i1Zg8lmpVoUDY9X_RMejlikQm58

# Frontend URL
FRONTEND_URL=http://localhost:3000





# WhatsApp Business API Configuration
WHATSAPP_API_URL=https://graph.facebook.com/v18.0
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_ACCESS_TOKEN=your_access_token
WHATSAPP_VERIFY_TOKEN=bizone_verify_token
WHATSAPP_WEBHOOK_URL=https://your-domain.com/api/webhooks/whatsapp
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WHATSAPP_APP_ID=your_app_id
WHATSAPP_APP_SECRET=your_app_secret


// In your callback page (/auth/google/callback)
useEffect(() => {
  const params = new URLSearchParams(window.location.search);
  const token = params.get('token');
  const success = params.get('success');
  
  if (success === 'true' && token) {
    // Store token and redirect to dashboard
    localStorage.setItem('auth_token', token);
    router.push('/dashboard');
  }
}, []);


social auth.tsx
const handleSocialLogin = (provider: string) => {
  if (provider === 'Google') {
    // Redirect to backend Google auth endpoint
    window.location.href = `${process.env.NEXT_PUBLIC_API_URL}/auth/google`;
  }
}